# EBO-SACCO Credit Scoring System — Dependencies
# Install: pip install -r requirements.txt
# Versions pinned to the environment the models were trained/pickled in.

# Core ML
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
xgboost==3.3.0
imbalanced-learn==0.14.2
shap==0.52.0
scipy==1.18.0

# Data I/O
openpyxl==3.1.5

# API
flask==3.1.3
flask-cors==6.0.5
gunicorn==23.0.0

# Training / analysis only (not needed by the API server)
matplotlib>=3.7.0
seaborn>=0.12.0
streamlit>=1.28.0
